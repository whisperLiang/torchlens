# TorchLens limitations

The canonical limitations and edge-scenario catalog moved to
[`docs/reference/limitations.md`](reference/limitations.md). The reference page lists each trigger,
the observable typed flag or reason, and the recommended remedy.

This compatibility path remains so existing links do not break.
